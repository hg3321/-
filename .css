body {
    margin: 0;
    padding: 0;
    background-color: black;
    overflow: hidden;
}

.stars-and-moon {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
}

.stars-and-moon::before {
    content: '';
    position: absolute;
    top: 50px;
    left: 50px;
    width: 80px;
    height: 80px;
    border-radius: 50%;
    background-color: white;
    box-shadow: 0 0 50px rgba(255, 255, 255, 0.5);
    z-index: 1;
}

.stars-and-moon::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: radial-gradient(circle at 50% 50%, rgba(255, 255, 255, 0.1) 0%, rgba(0, 0, 0, 0) 70%);
    background-size: 100px 100px;
    background-repeat: repeat;
    opacity: 0.5;
    z-index: 1;
}

.love-message {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    font-size: 48px;
    color: red;
    font-weight: bold;
    text-align: center;
    z-index: 2;
}